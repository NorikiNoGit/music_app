version: '3.8'

services:
  flask:
    build: .
    container_name: localtube
    ports:
      - "8080:8080"
    volumes:
    - ${SHARE_PATH}:/musicshare
    networks:
      - webnet

networks:
  webnet:
